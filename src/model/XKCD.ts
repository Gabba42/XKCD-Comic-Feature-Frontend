export class XKCDComic {

 private num: number;
 private year: string;
 private title: string;
 private alt: string;
 private img: string;

 constructor(num: number, year: string, title: string, alt: string, img: string) {
   this.num = num;
   this.year = year;
   this.title = title;
   this.alt = alt;
   this.img = img;
 }

}
